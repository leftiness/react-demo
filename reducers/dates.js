const date = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

const dates = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default dates;
